# =============================================================================
# Family Health Manager — Python Dependencies
# Install with: pip install -r requirements.txt
# Auth Stack: Google OAuth2 (authlib) + JWT (python-jose)
# LLM Stack: Groq (4 models) + NVIDIA NIM (embeddings) + Pinecone (vector DB)
# =============================================================================

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9

# ── Database ──────────────────────────────────────────────────────────────────
sqlmodel>=0.0.19
psycopg2-binary>=2.9.9
alembic>=1.13.1
supabase>=2.7.4

# ── Authentication ────────────────────────────────────────────────────────────
authlib>=1.3.0               # Google OAuth2 PKCE flow
python-jose[cryptography]>=3.3.0   # JWT token signing & verification
bcrypt>=4.0.0                # Password hashing

# ── LLM / AI Orchestration ───────────────────────────────────────────────────
langchain>=0.2.16
langchain-community>=0.2.16
langchain-core>=0.2.38
langgraph>=0.2.0

# ── Groq (Free-Tier LLM — 4 agents use 4 different Groq models) ──────────────
# Supervisor: llama-3.1-8b-instant  | Vitals:   llama-3.3-70b-versatile
# Generator:  llama-3.3-70b-versatile | Notifications: gemma2-9b-it
# Get your free API key at: https://console.groq.com/keys
langchain-groq>=0.1.9
groq>=0.9.0

# ── NVIDIA NIM (Free-Tier Embeddings — RAG Node only) ────────────────────────
# Model: nvidia/nv-embedqa-e5-v5 (dim=1024, purpose-built for RAG)
# Get your free API key at: https://build.nvidia.com/
langchain-nvidia-ai-endpoints>=0.3.0

# ── Vector Databases ──────────────────────────────────────────────────────────
pinecone>=5.0.0               # Pinecone v3+ modern client (free-tier cloud)
langchain-pinecone>=0.1.3     # Official LangChain↔Pinecone integration
# chromadb>=0.5.3             # Disabled — using Pinecone; re-enable only if switching to local ChromaDB

# ── Document Loaders & Text Splitting ─────────────────────────────────────────
pypdf>=4.2.0
tiktoken>=0.7.0

# ── Background Task Scheduling ────────────────────────────────────────────────
APScheduler>=3.10.4

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic-settings>=2.3.1
python-dotenv>=1.0.1

# ── Utilities ─────────────────────────────────────────────────────────────────
httpx>=0.27.0
twilio>=8.4.0

# ── PDF & Image Processing ────────────────────────────────────────────────
pymupdf>=1.24.0              # PyMuPDF (fitz) — PDF to image rendering
Pillow>=10.0.0               # Image compression (resize + JPEG quality) for vision API
